import { Button } from '@folio/stripes/components';
import React from 'react';

export const PrintPullSlip = () => <Button buttonStyle="primary mega" fullWidth to="pullslip">Print pull slip</Button>;
export const okEslint = null;
